## Aliases

Aliases are put at `$HOME/.customrc`.


| Alias               | Command                |
| ------------------- | ---------------------- |
| `pbcopy`            | `xclip -out -sel clip` |
| `pbpaste`           | `xclip -in -sel clip`  |

